pub mod parser;
pub mod renderer;
pub use parser::csv;
pub use parser::document;
pub use parser::types;
pub use renderer::svg;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
